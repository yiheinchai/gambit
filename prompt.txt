/loop 5min Run the next experiment from research_log.md OR implement design screens OR improve app features. Priority order: (1) If a training experiment is queued in research_log.md, start it in background shell (run_in_background=true) with time estimation and log observability. (2) While training runs, implement screens from design/design_handoff_missedtake/ RELIGIOUSLY — pixel-accurate, exact spacing, exact colors, exact typography. Follow the design README and JSX specs as the source of truth. (3) If all design screens are implemented and working, imagine yourself as a chess player using the app: walk through the journey step by step (enter username → wait for analysis → see weaknesses → drill → check progress) and identify the most impactful feature improvement or UX friction to fix. (4) When training finishes, run SAE pipeline, export ONNX, deploy to GitHub Pages, update research_log.md. CRITICAL: Never block or wait idle. Always run training as background shell and work on design/app in parallel. Check background task status regularly via ps/log files between design work.